<!DOCTYPE html>
<html>
  <head>
    <title>FriendShip</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://use.fontawesome.com/releases/v5.15.3/js/all.js" crossorigin="anonymous"></script>
    <link href="https://fonts.googleapis.com/css?family=Lora:400,700,400italic,700italic" rel="stylesheet" type="text/css" />
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:300italic,400italic,600italic,700italic,800italic,400,300,600,700,800" rel="stylesheet" type="text/css" />
  </head>

  <body>
    <% flash.each do |key, value| %>
      <%= content_tag(:div, value, class: "#{key}") %>
    <% end %>

    

    <%= yield %>
  </body>

  <footer>
    <% if logged_in? %>
      <%= link_to "Profile", user_path(current_user.id), class: "btn btn-primary mt-2" %>
      <%= link_to "Logout", session_path(current_user.id), method: :delete, class: "btn btn-primary ml-1 mt-2" %>
    <% else %>
      <%= link_to "Sign up", new_user_path, class: "btn btn-primary mt-2" %>
      <%= link_to "Login", new_session_path, class: "btn btn-primary ml-1 mt-2" %>
    <% end %>
  </footer>
</html>
